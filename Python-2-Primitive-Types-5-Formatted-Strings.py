# Formatted-Strings 

first = "Astra"
last = "Rai"
full = f"{first} {last}" # Astra Rai
full_one = f"{len(first)} {2 + 2}" # 5 4
print(full)
print(full_one)

# you can put any valid expressing in-between the {} braces

# old way to concatenate vars using + operator and empty string, full = first + " " +  last
# print(full)